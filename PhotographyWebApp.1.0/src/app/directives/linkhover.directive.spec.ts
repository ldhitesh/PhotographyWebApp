import { LinkhoverDirective } from './linkhover.directive';

describe('LinkhoverDirective', () => {
  it('should create an instance', () => {
    const directive = new LinkhoverDirective();
    expect(directive).toBeTruthy();
  });
});
